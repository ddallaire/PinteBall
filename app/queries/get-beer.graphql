query Beer($beerId: Int!) {
  beer(id: $beerId) {
    id
    name
    description
    ibu
    alcoholPercent
    imagePath
    breweries {
      id
      name
    }
    tags {
      id
      name
    }
    rating
    style
  }
}
